import { describe, expect, it } from "vitest";

describe("Tests", () => {
    it("1", () => {
        expect(1).toStrictEqual(1);
    });
    it("2", () => {
        expect(2).toStrictEqual(2);
    });
});
